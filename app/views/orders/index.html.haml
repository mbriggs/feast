%h1
  Nulogy Bar-b-que
  %nav
    %a(href='/orderers') Orderers
    \-
    %a(href='/non-orderers') Non Orderers
    \-
    %a(href='/order') This Weeks Order

= render 'new'

- unless @orders.empty?
  %h4 Previous Orders
  %ul
    - @orders.each do |order|
      %li
        = order.description
        %span= order.created_at
